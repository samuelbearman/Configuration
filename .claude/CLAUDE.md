# Coding Philosophy

1. Do not add preemptive features — only build what is explicitly needed now.
2. No premature abstractions — only extract a pattern after writing it three or more times.
3. Prefer explicit and readable code over clever one-liners.
4. Keep comments minimal — code should be self-documenting; only comment the non-obvious.
