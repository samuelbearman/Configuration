# Vim Cheatsheet

`t(` - move forward to just before char (replace the paren with any char that you want)
`T(` - move backward to just before char (replace the paren with any char that you want)
`r` - replace under cursor, type after to replace
`f(` - move forward to char (replace the paren with any char that you want)
`F(` - move backward to char (replace the paren with any char that you want) 
`cw` - change word, delete word from cursor and put in insert mode
`0` - Jump to first column in line, normal mode
`I` - Jump to first char in line in insert mode
`A` - Jump to end of line in insert mode
`$` - Jump to end of line in normal mode
`gg` - Jump to first line of file
`G` - Jump to last line of file